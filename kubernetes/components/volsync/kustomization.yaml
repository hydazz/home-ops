---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./externalsecret.yaml
  - ./pvc.yaml
  - ./replicationdestination.yaml
  # Kopiur owns new snapshots during migration; retain the destination and PVC
  # until Kopiur has produced verified recovery points.
  # - ./replicationsource.yaml
