---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrepository-source-v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: portainer
spec:
  interval: 1h
  url: https://portainer.github.io/k8s
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app portainer
spec:
  interval: 1h
  chart:
    spec:
      chart: portainer
      version: 1.0.66
      sourceRef:
        kind: HelmRepository
        name: portainer
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    image:
      repository: portainer/portainer-ee
      tag: 2.31.3@sha256:46e4896a069a7f3d1ab1533a019c819ab7b387f0b936d230579520a5f8bb688f
    service:
      type: ClusterIP
    persistence:
      existingClaim: *app
