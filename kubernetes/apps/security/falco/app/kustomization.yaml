---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
configMapGenerator:
  - name: falco-configmap
    files:
      - ./resources/falco.yaml
      - ./resources/falcoctl.yaml
      - ./resources/rules.yaml
generatorOptions:
  disableNameSuffixHash: true
