---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: authentik-secret
    template:
      data:
        AUTHENTIK_POSTGRESQL__HOST: postgres-rw.databases.svc.cluster.local
        AUTHENTIK_POSTGRESQL__NAME: authentik
        AUTHENTIK_POSTGRESQL__USER: "{{ .DB_USERNAME }}"
        AUTHENTIK_POSTGRESQL__PASSWORD: "{{ .DB_PASSWORD }}"
        AUTHENTIK_SECRET_KEY: "{{ .SECRET_KEY }}"
        GEOIP_ACCOUNT_ID: "{{ .GEOIP_ACCOUNT_ID }}"
        GEOIP_LICENSE_KEY: "{{ .GEOIP_LICENSE_KEY }}"
        AUTHENTIK_STORAGE__MEDIA__BACKEND: "s3"
        AUTHENTIK_STORAGE__MEDIA__S3__ACCESS_KEY: "{{ .AWS_ACCESS_KEY_ID }}"
        AUTHENTIK_STORAGE__MEDIA__S3__SECRET_KEY: "{{ .AWS_SECRET_ACCESS_KEY }}"
        AUTHENTIK_STORAGE__MEDIA__S3__BUCKET_NAME: authentik
        AUTHENTIK_STORAGE__MEDIA__S3__REGION: au-melb-1
        AUTHENTIK_STORAGE__MEDIA__S3__ENDPOINT: https://s3.hyde.services
        AUTHENTIK_STORAGE__MEDIA__S3__CUSTOM_DOMAIN: s3.hyde.services/authentik
        AUTHENTIK_EMAIL__FROM: "authentik <{{ .EMAIL_USER }}>"
        AUTHENTIK_EMAIL__HOST: mail.hyde.services
        AUTHENTIK_EMAIL__PASSWORD: "{{ .EMAIL_PASSWORD }}"
        AUTHENTIK_EMAIL__PORT: "587"
        AUTHENTIK_EMAIL__TIMEOUT: "30"
        AUTHENTIK_EMAIL__USE_SSL: "false"
        AUTHENTIK_EMAIL__USE_TLS: "true"
        AUTHENTIK_EMAIL__USERNAME: "{{ .EMAIL_USER }}"
  dataFrom:
    - extract:
        key: authentik
    - extract:
        key: mail-hs
