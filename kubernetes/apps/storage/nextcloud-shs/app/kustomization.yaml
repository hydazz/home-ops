---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
  - ./ocirepository.yaml
configMapGenerator:
  - name: nextcloud-shs-config
    files:
      - ./resources/custom.config.php
    options:
      disableNameSuffixHash: true
  - name: nextcloud-shs-php-configmap
    files:
      - ./resources/nextcloud.ini
      - ./resources/www.conf
  - name: nextcloud-shs-nginx-configmap
    files:
      - ./resources/default.conf
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
